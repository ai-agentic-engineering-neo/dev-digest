import type { SkillType } from "@devdigest/shared";

/** Type-chip colors, per the design. */
export const TYPE_COLORS: Record<SkillType, string> = {
  rubric: "#3b82f6",
  convention: "#10b981",
  security: "#ef4444",
  custom: "#999999",
};
