import { SkillsView } from "../_components/SkillsView";

/* Route: /skills/new (create form in the right pane). Thin route entry. */
export default function NewSkillPage() {
  return <SkillsView mode="new" />;
}
