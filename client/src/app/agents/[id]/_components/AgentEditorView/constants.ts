/** Editor tabs reachable via ?tab= (Part-0 ships Config only). */
export const VALID_TABS: readonly string[] = ["config"];

/** Tab shown when ?tab= is missing or unknown. */
export const DEFAULT_TAB = "config";
