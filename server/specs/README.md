# server/specs/

Feature/requirement specs for this package — what a module must do, written
before implementation (one file per feature). Link a new spec from
[`../CLAUDE.md`](../CLAUDE.md) under "Read when" once it exists.
