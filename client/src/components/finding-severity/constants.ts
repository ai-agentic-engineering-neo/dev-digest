/** Popover panel width (px). */
export const POPOVER_WIDTH = 380;
/** Max popover height before it scrolls (px). */
export const POPOVER_MAX_HEIGHT = 440;
/** Gap between trigger and panel (px). */
export const POPOVER_GAP = 6;
/** Grace period so the pointer can travel from trigger into the panel (ms). */
export const POPOVER_CLOSE_DELAY_MS = 120;
