# reviewer-core/docs

Deeper design notes for the review engine that don't belong in
[`../CLAUDE.md`](../CLAUDE.md) (kept short by design) or the package
[`README.md`](../README.md) (kept to what's needed to run and test it).

Empty for now — add a file per topic as it comes up (e.g. a grounding
algorithm writeup, a prompt-assembly design note, a structured-output repair
strategy).
