import { SkillsView } from "./_components/SkillsView";

/* Route: /skills (Skills Lab, nothing selected). Thin route entry — the
   master-detail view lives in _components/SkillsView. */
export default function SkillsPage() {
  return <SkillsView />;
}
