/** Donut segment colours for "Findings by category" — cycled by index since
   category names are server-defined free text, not a fixed enum. */
export const CATEGORY_COLORS = ["#3b82f6", "#8b5cf6", "#ef4444", "#f59e0b", "#10b981", "#ec4899"];
