/** Hover intent before the popover opens (ms). */
export const OPEN_DELAY_MS = 150;

/** Grace period to move the pointer from the chips onto the popover (ms). */
export const CLOSE_GRACE_MS = 100;

export const POPOVER_WIDTH = 420;
export const POPOVER_MAX_HEIGHT = 400;

/** Gap between the chips and the popover, and the minimum viewport margin (px). */
export const POPOVER_GAP = 6;
export const VIEWPORT_MARGIN = 8;
