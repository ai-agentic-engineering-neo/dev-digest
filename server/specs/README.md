# server/specs

Feature and design specs for work landing in `@devdigest/api` — written before
(or alongside) an implementation, distinct from the API's runtime Zod
contracts in `src/vendor/shared`.

Empty for now — add one file per spec (e.g. `repo-intel-v2.md`,
`multi-agent-runs.md`).
