# server/docs/

Deeper architecture notes and decisions that don't fit `../README.md`'s
top-level map — one file per topic (e.g. `repo-intel-indexing.md`,
`grounding-gate.md`). Link a new file from [`../CLAUDE.md`](../CLAUDE.md)
under "Read when" once it exists; don't duplicate what `../README.md` already
covers.
