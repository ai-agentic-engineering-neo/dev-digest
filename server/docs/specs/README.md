# server/docs/specs

Feature specifications generated by the `sdd-1-generate-spec` skill.

Naming convention: `[NN]-spec-[feature-name]/[NN]-spec-[feature-name].md` (zero-padded sequence, lowercase-hyphen name).
