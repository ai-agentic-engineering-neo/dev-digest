# Architecture

> **Stub.** Not written yet. Referenced from the repo-root `CLAUDE.md`.

**Purpose:** The end-to-end review pipeline across all four packages, in one place.

**What belongs here:** add-repo → clone → index → import PR → run review → grounding → persist → SSE; where each step lives and which package owns it.

**What does not belong here:** anything already covered by the module `README.md`,
and anything volatile enough to go stale within a lesson or two.
