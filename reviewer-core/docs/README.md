# docs — reviewer-core

Deep-dives for the `reviewer-core` package. Linked from
`reviewer-core/CLAUDE.md` › *Read when*.

- **[`pipeline.md`](pipeline.md)** — the stages from diff to grounded findings:
  mode selection, prompt assembly and the injection guard, structured output,
  reduce, the citation gate, scoring — plus why the package emits no JS and
  stays free of I/O.

Related: reviewer system prompts are documented in
[`../../docs/agent-prompts/`](../../docs/agent-prompts/).
