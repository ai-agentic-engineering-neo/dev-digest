# Repo intel

> **Stub.** Not written yet. Referenced from `server/CLAUDE.md`.

**Purpose:** The codebase indexer that produces the repo map fed into review prompts.

**What belongs here:** the `RepoIntel` facade contract, the full/incremental pipelines, ast-grep → import graph → PageRank → repo map, the degraded contract, `INDEXER_VERSION`.

**What does not belong here:** anything already covered by the module `README.md`,
and anything volatile enough to go stale within a lesson or two.
