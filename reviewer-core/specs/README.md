# specs — reviewer-core

Written feature specs for the `reviewer-core` package, read before implementing
one. Linked from `reviewer-core/CLAUDE.md` › *Read when*.

- **[`review-contract.md`](review-contract.md)** — the public engine contract:
  `ReviewInput` / `ReviewOutcome` field by field, mode-selection rules, the
  grounding rules, the score formula, verdict and blocker semantics,
  cancellation, and the purity rules that are contract rather than style.
