# docs

Deep dives for this package that are too long for `../README.md`
(design notes, sequence diagrams, decision records).

`../README.md` stays the source of truth: link to files here from it,
and add a "Read when …" line to `../CLAUDE.md` for anything an agent must read.
