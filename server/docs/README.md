# server/docs

Deeper design and architecture notes for `@devdigest/api` that don't belong in
[`../CLAUDE.md`](../CLAUDE.md) (kept short by design) or the package
[`README.md`](../README.md) (kept to what's needed to run and test it).

Empty for now — add a file per topic as it comes up (e.g. a specific adapter's
design, a migration strategy writeup, a DI container deep-dive).

Related: the built-in reviewer agent prompts live in the repo-root
[`docs/agent-prompts/`](../../docs/agent-prompts/), mirrored in
`src/db/seed-prompts.ts`.
