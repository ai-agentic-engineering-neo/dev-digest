# docs — client

Deep-dives for the `client` package. Linked from `client/CLAUDE.md` › *Read when*.

- **[`ui-architecture.md`](ui-architecture.md)** — the Server/Client component
  boundary, the provider stack, the error-UX taxonomy, the data flow from
  `api.ts` through the hooks (with the cache-key table), live runs over SSE, and
  the component/styling conventions.

Related: the route → API map is in [`../README.md`](../README.md); the UI kit has
its own notes in [`../src/vendor/ui/README.md`](../src/vendor/ui/README.md).
