# client/specs

Feature and design specs for work landing in `@devdigest/web` — written before
(or alongside) an implementation.

Empty for now — add one file per spec (e.g. `agent-editor-v2.md`,
`run-trace-live-log.md`).
