# client/docs

How the studio works **today** — deep dives too long for `../README.md`.

Good candidates: TanStack Query keys and invalidation, global error toasts, the
app shell and `g`-then-key shortcuts, the next-intl message workflow, diff
rendering, styling tokens and theming.

Not here: the UI route map (`../README.md`), intent for unbuilt screens
(`../specs/`), lessons learned (`../INSIGHTS.md`).

Files:

- [`ui-architecture.md`](ui-architecture.md) — Server/Client Component boundaries,
  providers, the network layer, hooks and query keys, SSE, i18n, styling, app shell.
