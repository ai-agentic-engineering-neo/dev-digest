# e2e/docs

Deeper design notes for `@devdigest/e2e` that don't belong in
[`../CLAUDE.md`](../CLAUDE.md) (kept short by design) or the package
[`README.md`](../README.md) (kept to what's needed to run the suite).

Empty for now — add a file per topic as it comes up (e.g. a hermetic-runner
design note, a flakiness postmortem).

Design/feature specs for this package live in [`specs/`](specs/) — not to be
confused with the package's top-level `../specs/`, which holds the
`*.flow.json` test-flow specs the suite actually runs.
