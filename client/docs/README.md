# client/docs

Deeper design and architecture notes for `@devdigest/web` that don't belong in
[`../CLAUDE.md`](../CLAUDE.md) (kept short by design) or the package
[`README.md`](../README.md) (kept to what's needed to run and test it).

Empty for now — add a file per topic as it comes up (e.g. a routing decision,
a state-management writeup, a design-system note).
