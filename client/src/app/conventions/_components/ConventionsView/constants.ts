/** Agent the extracted skill is linked to by default (name match, first agent otherwise). */
export const DEFAULT_LINK_AGENT_NAME = "General Reviewer";

/** Rough token estimate for the modal's body counter (chars / 4). */
export const CHARS_PER_TOKEN = 4;
